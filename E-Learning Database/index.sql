USE finpro
CREATE CLUSTERED INDEX customer_id
ON FsSeminar(CustomerId);  

CREATE CLUSTERED INDEX staff_id
ON FsDiscussion (part_id);  

CREATE CLUSTERED INDEX ix_parts_id
ON FsOnsite(part_id);  

CREATE CLUSTERED INDEX ix_parts_id
ON production.parts (part_id);  

CREATE CLUSTERED INDEX ix_parts_id
ON production.parts (part_id);  

CREATE CLUSTERED INDEX ix_parts_id
ON production.parts (part_id);  

CREATE CLUSTERED INDEX ix_parts_id
ON production.parts (part_id);  